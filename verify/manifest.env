# Contract for the host e2e verification loop.
# One KEY=value per line — sourced by e2e-verify.
APP_PORT=8010
COMPOSE_FILE=docker-compose.yml
HEALTH_URL=http://localhost:8010/healthz
API_BASE_URL=http://localhost:8010
UP_TIMEOUT=30
DOWN_TIMEOUT=10
DEPLOY_TIMEOUT=60
